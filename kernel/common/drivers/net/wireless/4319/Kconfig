config BCM_4319_DRIVER
	bool "BCM_4319_DRIVER"
	depends on MMC
	---help---
	  This module adds support for wireless adapters based on
	  BCM4319 chipset.
	  If you choose to build a module, it'll be called bcm4319. Say M if
	  unsure.


